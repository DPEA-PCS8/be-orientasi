spring:
  application:
    name: orientasi
    
  # Database Configuration
  datasource:
    url: jdbc:sqlserver://${DB_HOST:10.226.60.31}:${DB_PORT:49705};databaseName=${DB_NAME:pcs8_orientasi};encrypt=true;trustServerCertificate=true
    username: ${DB_USERNAME:admin.pcs8}
    password: ${DB_PASSWORD:SQL2022}
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true 
    
  # LDAP Configuration
  ldap:
    urls: ldap://10.225.60.10:389
    base: dc=devojk,dc=go,dc=id

server:
  port: 8080
  servlet:
    context-path: /api

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:your-256-bit-secret-key-here-change-this-in-production-min-32-chars}
  expiration: ${JWT_EXPIRATION:86400000}  # 24 hours in milliseconds

# AES Encryption Configuration
aes:
  encryption:
    key: ${AES_ENCRYPTION_KEY:}  # Base64-encoded 256-bit AES key. Harus diisi untuk production.

# API Security Configuration
app:
  api-key: ${API_KEY:da39b92f-a1b8-46d5-a10c-d08b1cc92218}

# Logging
logging:
  level:
    com.pcs8.orientasi: INFO
    org.springframework.security: DEBUG